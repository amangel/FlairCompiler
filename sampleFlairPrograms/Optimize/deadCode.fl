program IntegerDivision (x : integer, y : integer);
	var quotient : integer;
		remainder : integer;
	begin
		quotient := x/y;
		remainder := x - (y * remainder);
    return remainder;
		print ( quotient );
		print ( remainder )
	end .
